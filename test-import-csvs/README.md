# Test Import CSVs

Generated by `generate_csvs.py`. Run `python3 generate_csvs.py` to regenerate all files.

## Import Order (follow exactly — dependencies go top to bottom)

| # | File | Rows | Page | Dependencies |
|---|------|------|------|--------------|
| 1 | `01_warehouses.csv` | 10 | Warehouses | none |
| 2 | `02_carriers.csv` | 15 | Carriers | none |
| 3 | `03_suppliers.csv` | 25 | Suppliers | none |
| 4 | `04_sales_channels.csv` | 15 | Settings → Channels | none (`default_warehouse_id` left blank) |
| 5 | `05_team_members.csv` | 15 | Settings → Team | none |
| 6 | `06_products.csv` | 100 | Inventory → Products | none |
| 7 | `07_inventory.csv` | 100 | Inventory | **warehouses + products must exist first** (warehouse placeholders auto-resolve by code) |
| 8 | `08_orders.csv` | 100 | Orders | **products must exist first (SKU matched)** |
| 9 | `09_shipments.csv` | 100 | Shipments | **orders + carriers + warehouses must exist first** (linked by `order_number`) |
| 10 | `10_finance.csv` | 10 | Finance | **carriers must exist first** (carrier matched by name) |

---

## Inventory CSV behavior

`07_inventory.csv` uses values like `REPLACE_WITH_UUID_FOR_WH-DEL-01` in `warehouse_id`.

The Inventory import button now auto-resolves these using existing warehouse `code` values, so manual UUID replacement is not required.

If import still fails, ensure:
- `01_warehouses.csv` was imported first.
- Warehouse `code` values exist and match (for example `WH-DEL-01`).
- `06_products.csv` was imported before inventory.

---

## Notes

- All data uses Indian context: cities, states, postal codes, +91 phone numbers, INR currency.
- Products span all 12 allowed categories.
- `07_inventory.csv` assigns each warehouse its own 10-product subset (no duplicates per warehouse).
- `08_orders.csv` includes explicit `order_number` values so downstream imports can link reliably.
- `09_shipments.csv` references those order numbers and represents historical shipment states such as delivered, in transit, returned, and exception.
- `10_finance.csv` uses `carrier_name` to validate the carrier link and checks that `final_amount = base_amount + adjustments - penalties`.
- `05_team_members.csv` uses non-existing email addresses to avoid conflicts with the preserved admin/superadmin users.
